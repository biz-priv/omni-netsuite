---
stateMachine:
  name: ${self:service}-netsuite-ap-vendor-state-machine-${self:provider.stage}
  # events:
  #   - schedule:
  #       rate: cron(0 14 * * ? *) # UTC:- 2:00 PM
  #   - schedule:
  #       rate: cron(0 20 * * ? *) #  UTC:- 8:00 PM
  definition:
    Comment: "State machine to update netsuit vendor internal details"
    StartAt: PublishEvents
    States:
      PublishEvents:
        Type: Task
        Resource: "arn:aws:lambda:#{AWS::Region}:#{AWS::AccountId}:function:${self:service}-netsuite-ap-vendor-${self:provider.stage}"
        Next: EndChoice
      EndChoice:
        Type: Choice
        Choices:
          - Variable: $.hasMoreData
            StringEquals: "false"
            Next: SuccessState
          - Variable: $.hasMoreData
            StringEquals: "true"
            Next: PublishEvents
      SuccessState:
        Type: Succeed
